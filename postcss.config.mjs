// Tailwind CSS v4 uses a single PostCSS plugin. Do NOT add autoprefixer or
// postcss-import here — both are built into @tailwindcss/postcss.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
