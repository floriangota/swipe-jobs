# photos (M4 — Photos & Moderation Pipeline)

Placeholder. Built in M4: upload (worker photo / employer logo) with type/size/
dimension validation, EXIF stripping, re-encode, private bucket, and the
pending -> approved moderation gate. Empty until then.
