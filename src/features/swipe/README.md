# swipe (M5 — Swipe Feed & Matching · THE CORE + FLAGSHIP)

Placeholder. Built in M5: the feed endpoint (city + category filtered, exclude
swiped, cursor pagination), the Framer Motion swipe engine (drag physics, tilt,
spring-back, velocity fling, optimistic UI), employer candidate stack, and
atomic race-safe match creation. The tentpole milestone. Empty until then.
