# notifications (M7 — Notifications)

Placeholder. Built in M7: transactional email via Resend (new match, new message
when offline, verification), in-app notification center + unread badges, and
background jobs (Vercel Cron). Empty until then.
