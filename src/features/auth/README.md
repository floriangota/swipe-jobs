# auth (M1 — Authentication & Accounts)

Placeholder. Built in M1: signup (worker/employer), login/logout, soft-gate email
verification, password reset (no-enumeration), sessions, `GET /me`, role route
protection. Empty until then.
